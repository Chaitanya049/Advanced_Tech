#include<iostream>
using namespace std;
int main()
{
    int a,b,ans=0;
    cout<<"enter a value"<<endl;
    cin>>a;
    cout<<"enter b value"<<endl;
    cin>>b;
    while(b>0)
    {
        if(a&1)
        {
            ans+=a;
        }
        b=b>>1;
        a=<<1;
    }
    cout<<ans;
}